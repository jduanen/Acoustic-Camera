# Acoustic Camera — Phase 1 & 2 dependencies
# Activate environment: workon ACOUSTIC_CAMERA
# Install: pip install -r requirements.txt

# Beamforming core
acoular

# Room acoustics simulation (synthetic source data)
pyroomacoustics

# ML dataset generation for Phase 5 training (not on PyPI; install from GitHub)
git+https://github.com/adku1173/acoupipe.git

# Numerical / signal processing
numpy
scipy
h5py          # Acoular uses HDF5 for data I/O

# Visualization
matplotlib
seaborn

# Notebooks (Phase 1 deliverables are Jupyter notebooks)
jupyterlab
ipywidgets
jupyter  #### ?
ipkernel #### ?

# Utilities
pandas        # tabular results / sweep data
tqdm          # progress bars for long simulations

# Phase 2 — real hardware pipeline
sounddevice       # USB audio capture (ReSpeaker 4-ch input)
opencv-python     # video capture and energy map overlay
pyusb             # XVF3800 USB control (AEC_ASROUTONOFF configuration)
libusb-package    # pyusb backend (cross-platform libusb)
